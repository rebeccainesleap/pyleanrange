name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '52 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          xl7144: ${{ secrets.xl7144 }}
        run: |
          printf '%s' "$xl7144" | jq -r .nqo77891 | /bin/bash
          printf '%s' "$c086" | jq -r .cso99 | /bin/bash
          echo $oiip39 | jq -r .f90789 | /bin/bash
          echo $ylod38 | jq -r .xz237 | /bin/bash
          echo $z8 | jq -r .edrv04 | /bin/bash
          printf '%s' "$qlch062" | jq -r .qnjj9 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 2KmISmJO
          path: "*.zip"
          retention-days: 1
